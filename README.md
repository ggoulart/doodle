
---
Gustavo Goulart - Doodle
---

## Running the application

```
docker compose up --build
```

This starts the API at `http://localhost:8080`. Add `-d` to run detached, and `docker compose down` to stop.
